<header class="container">
    {{#if session.isAuthenticated}}
        {{user-handle session=session}}
    {{/if}}
</header>

{{outlet}}

<footer>
    <div class="container">
        <p>{{t 'app.infamc_copyrights'}}</p>
        <p>{{t 'app.ankama_copyrights'}}</p>
    </div>
</footer>
import { isWeekend } from 'dofus-workbench/helpers/is-weekend';
import { module, test } from 'qunit';

module('Unit | Helper | is weekend');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isWeekend([42]);
  assert.ok(result);
});

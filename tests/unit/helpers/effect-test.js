import { effect } from 'dofus-workbench/helpers/effect';
import { module, test } from 'qunit';

module('Unit | Helper | effect');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = effect([42]);
  assert.ok(result);
});

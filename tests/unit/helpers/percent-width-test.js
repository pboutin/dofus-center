import { percentWidth } from 'dofus-workbench/helpers/percent-width';
import { module, test } from 'qunit';

module('Unit | Helper | percent width');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = percentWidth([42]);
  assert.ok(result);
});
